package main

func main() {
	// fmt.Println("start")
	// logger := logging.GetLogger("info")
	// cfg := config.GetConfig()
	// pgConfig := psqlClient.NewPgConfig(
	// 	cfg.PostgreSql.Username,
	// 	cfg.PostgreSql.Password,
	// 	cfg.PostgreSql.Host,
	// 	cfg.PostgreSql.Port,
	// 	cfg.PostgreSql.Dbname)
	// pgClient, err := psqlClient.NewClient(context.Background(), 5, time.Second*5, pgConfig)
	// if err != nil {
	// 	logger.Fatal(err)
	// }
	// repo := postgres.NewVoteStorage(pgClient, logger)
	// rdCfg := rdclient.NewRdConfig(cfg.Redis.Password, cfg.Redis.Host, cfg.Redis.Port, cfg.Redis.DbNumber)
	// client, err := rdclient.NewClient(context.Background(), &rdCfg)
	// if err != nil {
	// 	logger.Fatal(err)
	// }

}
